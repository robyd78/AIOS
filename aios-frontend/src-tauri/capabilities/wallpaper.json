{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "aios.wallpaper",
  "description": "Allow picking and showing wallpapers",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:default"
  ]
}
