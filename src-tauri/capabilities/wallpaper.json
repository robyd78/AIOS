{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "aios.wallpaper",
  "description": "Picker + read for wallpapers",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "fs:default"
  ]
}
